pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                dir('C:/Jenkins/test.bat') {
                    /* execute commands in the scripts directory */
                }
            bat 'C:/Jenkins/test.bat'
            }
        }
        stage('Test') {
            steps {
                dir('C:/Jenkins/test.bat') {
                    /* execute commands in the scripts directory */
                }
                bat 'C:/Jenkins/test.bat'
            }
        }
      stage('Package') {
            steps {
                dir('C:/Jenkins/test.bat') {
                    /* execute commands in the scripts directory */
                }
                bat 'C:/Jenkins/test.bat'
            }
        }
        stage('Deploy') {
            steps {
                dir('C:/Desktop/Jenkins/test.bat') {
                    /* execute commands in the scripts directory */
                }
                bat 'C:/Desktop/Jenkins/test.bat'
            }
        }
    }
}
